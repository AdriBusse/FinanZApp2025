export interface IUpdateExpenseCategory {
  id: string;
  name?: string;
  color?: string;
  icon?: string;
}
export interface IReturn {
  id: string;
  name: string;
  color: string;
  icon: string;
}
